A file has been flagged as inappropriate.

filename: '${f.filename}'
file URL: ${f.url(qualified=True)}

parent: ${f.parent.title}
parent URL: ${f.parent.url(qualified=True)}

reporter: ${email}

reason:
${reason}

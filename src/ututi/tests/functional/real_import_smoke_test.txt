A test of all the import views together with an actual snapshot of ututi
========================================================================

Log in as admin:

    >>> browser = Browser()
    >>> form = browser.getForm('login_form')
    >>> form.getControl('Email').value = "admin@ututi.lt"
    >>> form.getControl('Password').value = "asdasd"
    >>> form.getControl('Login').click()

Go to admin page:

    >>> browser.open('http://localhost/admin')

And upload all the files one by one:

    >>> from ututi.tests.functional import import_csv
    >>> import_csv(browser, 'user_import', 'export_users.csv')
    >>> import_csv(browser, 'groups_import', 'export_groups.csv')
    >>> import_csv(browser, 'structure_import', 'export_structure.csv')
    >>> import_csv(browser, 'subjects_import', 'export_subjects.csv')
    >>> import_csv(browser, 'group_members_import', 'export_group_members.csv')
    >>> import_csv(browser, 'user_logos_import', 'export_user_logos.csv')
    >>> import_csv(browser, 'group_logos_import', 'export_group_logos.csv')
    >>> import_csv(browser, 'structure_logos_import', 'export_structure_logos.csv')
    >>> import_csv(browser, 'group_files_import', 'export_group_files.csv')
    >>> import_csv(browser, 'subject_files_import', 'export_subject_files.csv')

    # >>> import_csv(browser, 'group_pages_import', 'export_group_pages.csv')
    # >>> import_csv(browser, 'group_watched_subjects_import', 'export_group_watched_subjects.csv')
    # >>> import_csv(browser, 'subject_pages_import', 'export_subject_pages.csv')
    # >>> import_csv(browser, 'user_ignored_subjects_import', 'export_user_ignored_subjects.csv')
    # >>> import_csv(browser, 'user_watched_subjects_import', 'export_user_watched_subjects.csv')

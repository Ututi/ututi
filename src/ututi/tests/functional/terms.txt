Terms of use
============

Testing Lithuanian pages.

    >>> import pylons.test
    >>> pylons.test.pylonsapp.config['tpl_lang'] = 'lt'
    >>> browser = Browser()
    >>> browser.open('http://localhost/terms')
    >>> browser.printQuery('//h2/text()')
    1. Turinio licencija
    2. Naudotojų atsakomybė ir teisės
    3. Garantijos atsisakymas
    4. Atsakomybės ribojimas
    5. Sutikimas su licencija (licencijos priėmimas)
    >>> browser.open('http://localhost/about')
    >>> browser.open('http://localhost/advertising')


Testing Polish pages.

    >>> pylons.test.pylonsapp.config['tpl_lang'] = 'pl'
    >>> browser = Browser()
    >>> browser.open('http://localhost/terms')
    >>> browser.printQuery('//h2/text()')
    1. Licencja treści w serwisie
    2. Odpowiedzialność i prawa użytkowników
    3. Wyłączenie odpowiedzialności
    4. Akceptacja regulaminu
    >>> browser.open('http://localhost/about')


Testing English pages.

    >>> from pylons import config
    >>> pylons.test.pylonsapp.config['tpl_lang'] = 'en'
    >>> browser = Browser()
    >>> browser.open('http://localhost/terms')
    >>> browser.open('http://localhost/about')
    >>> browser.open('http://localhost/advertising')

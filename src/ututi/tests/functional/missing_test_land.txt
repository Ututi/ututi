Pages that are not visited even once during a test run
======================================================

I am visiting them so they would get translated properly.

    >>> browser = Browser.logIn()
    >>> browser.open('http://localhost/bugs')
    >>> browser.open('http://localhost/group/moderators/files')
    >>> browser.open('http://localhost/profile/files')
    >>> browser.open('http://localhost/tour')

Polish frontpage:
    >>> from pylons import config
    >>> config['tpl_lang'] = 'pl'
    >>> browser = Browser()
    >>> browser.open('http://localhost/')
    >>> browser.open('http://localhost/join')

Group search (not removed yet):
    >>> browser = Browser.logIn()
    >>> browser.open('http://localhost/findgroup')
    >>> browser.url
    'http://localhost/findgroup'
